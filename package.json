{
  "name": "sawahsense-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F sawahsense dev",
    "dev:backend": "cd backend && uv run uvicorn main:app --reload",
    "build": "pnpm -F sawahsense build"
  }
}
